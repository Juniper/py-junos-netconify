system {
    host-name {{ hostname }};
    domain-name {{ domain }};
    root-authentication {
        encrypted-password "$1$Ksi/alXi$s6xGSft5M8PMIxTwwUG3o1"; ## SECRET-DATA
    }
    login {
        user jeremy {
            class super-user;
            authentication {
                encrypted-password "$1$TE2wazbV$HNcj1.CjSuqSa7VnpicAH/"; ## SECRET-DATA
            }
        }
    }
    services {
        ssh;
        netconf {
            ssh;
        }
    }
}
interfaces {
    {{ eth }} {
        unit 0 {
            family inet {
{%- if ipaddr == 'dhcp' %}            
                dhcp;
{%- else %}
                address {{ ipaddr }};
{%- endif %}                
            }
        }
    }
}
security {
    zones {                             
        functional-zone management {
            interfaces {
                {{ eth }};
            }
            host-inbound-traffic {
                system-services {
                    all;
                }
                protocols {
                    all;
                }
            }
        }
    }
}
